# Obtained from dashboard.privy.io → Settings → API keys.
# The secret never leaves the server: policies and wallets are created server-side, never
# from the browser.
PRIVY_APP_ID=
PRIVY_APP_SECRET=
